package OGFracCalc;

public class Storage {

	public static void main(String[] args) {
//		Checkpoint 2
	    /* public static String produceAnswer(String input) {
	    	String[] split_input = input.split(" ");
	    	// Take second operand
	    	String operand = split_input[2];
	    	// Prime Variables
	    	int whole = 0;
	    	int numerator = 0;
	    	int denominator = 0;
	    	if(operand.contains("_") && operand.contains("/")) { // Case of mixed number
	    		whole = Integer.parseInt(operand.split("_")[0]);
	    		numerator = Integer.parseInt((operand.split("_")[1]).split("/")[0]);
	    		denominator = Integer.parseInt((operand.split("_")[1]).split("/")[1]);
	    	} else if(operand.contains("/")) { // Case of fraction
	    		whole = 0;
	    		numerator = Integer.parseInt(operand.split("/")[0]);
	    		denominator = Integer.parseInt(operand.split("/")[1]);
	    	} else { // Case of whole number
	    		whole = Integer.parseInt(operand);
	    		numerator = 0;
	    		denominator = 1;
	    	}
	    	String answer = "whole:" + whole +" numerator:" + numerator + " denominator:" + denominator;
	    	return answer;
	    } */
	}

}
